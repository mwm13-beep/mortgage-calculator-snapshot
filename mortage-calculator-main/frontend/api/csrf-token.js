// import crypto from 'crypto';

// export function getCsrfToken(req, res) {
//   const token = crypto.randomBytes(32).toString('hex');
//   res.cookie('csrfToken', token, { httpOnly: true, sameSite: 'strict', secure: true });
//   res.json({ csrfToken: token });
// }
